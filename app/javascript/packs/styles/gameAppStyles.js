const gameAppStyles = (theme) => ({
    card: {
        textAlign: 'center',
        flexWrap: 'wrap'
    },
    actions: {
        display: 'flex',
        justifyContent: 'center'
    }
});

export default gameAppStyles;