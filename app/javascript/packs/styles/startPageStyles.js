const startPageStyles = (theme) => ({
  card: {
    marginTop: '40%',
    textAlign: 'center',
  },
  button: {
    margin: 'auto',
    width: '50%',
    marginBottom: '15px'
  }
});

export default startPageStyles;
