const leaderboardStyles = (theme) => ({
    root: {
        width: '100%',
        overflowX: 'auto',
    },
    heading: {
        marginBottom: '2%',
    },
    table: {
        minWidth: 500
    },
    cell: {
        textAlign: 'center'
    }
});

export default leaderboardStyles;